open React

let[@react.component] make ~(children : unit -> React.element) () =
  let is_mounted, set_mounted = useState (fun () -> false) in
  useEffect (fun () ->
      let () = set_mounted (fun _ -> true) in
      None ) ;
  if is_mounted then children () else null
